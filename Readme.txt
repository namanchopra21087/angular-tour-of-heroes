*Set up Visual studio code for angular IDE.
*Install node.js on local machine.
*Use npm install (npm is the package manager for all the libraries used for UI development).
*Use npm install in the workspace folder.
*Start using ng commands like ng serve --open.